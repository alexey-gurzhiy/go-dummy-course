
// The package is sorting BubleSort and then making a binary search
package Sort

import (
	"fmt"
	// "testing"
)

var maxNum int = 45
var selectNum int = 45

func ExampleSort() {

	result := StartSort(maxNum, selectNum)
	result = result
	fmt.Println(result)
	// output: Ваше число записано в слайсе под номером 1
}
 